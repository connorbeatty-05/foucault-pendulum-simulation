# foucault-pendulum-simulation
model the Foucault pendulum using dimensionless coupled equations, convert the second-order system into a first-order system, solve it with RK4, compare Rossby numbers R0 =10,2,1, and verify the solver with convergence/Richardson error estimates.
